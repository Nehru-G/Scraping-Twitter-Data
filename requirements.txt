streamlit==0.83.0
datetime
snscrape==0.3.5
pandas==1.2.4
pymongo==3.12.0

# You can install the dependencies from the requirements.txt file using the following command:

# pip install -r requirements.txt

# Make sure to run this command in the terminal or command prompt while being in the same directory as the requirements.txt file.
This will install all the required dependencies for your project.
